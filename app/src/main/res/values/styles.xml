<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOrangeLBC</item>
        <item name="colorPrimaryDark">@color/colorOrangeLBC</item>
        <item name="colorAccent">@color/colorOrangeLBC</item>
        <item name="colorControlActivated">@color/colorOrangeLBC</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">
            @drawable/splashscreen
        </item>
    </style>
</resources>
